<div class="container">
    <h2>Here are the details for <%= @user.username %>:</h3>
        <div>
            <h3>Email: </h2><p><%= @user.email %></p>
            <h3>Status: </h2><p><%= @user.active ? "Active" : "Inactive" %></p>
        </div>
</div>

<div class="buttons">
    <%= link_to "Back to All Users", users_path, class: "btn-flat" %>
</div>

<div class="buttons">
    <%= link_to "Delete", users_auth_password_path, class: "btn-flat" %>
</div>